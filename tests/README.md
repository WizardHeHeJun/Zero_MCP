# tests

Zero_MCP 的测试：Python 侧 `pytest`（节点契约、边界、默认关零回归、`importorskip` 跳过重依赖）。TS 侧 MCP 服务层测试在 `mcp-server/`（vitest / `node:test`）。

> 当前为**空骨架**，随实现补用例。跑测试前需先建 conda 环境 `ai-harness`（见根 `CLAUDE.md` / `environment.yml`）。
